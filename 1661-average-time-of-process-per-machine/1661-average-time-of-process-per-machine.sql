/* Write your PL/SQL query statement below */
select m.machine_id as machine_id, round(avg(p.timestamp - m.timestamp),3) as processing_time from Activity m join Activity p on m.machine_id=p.machine_id and m.process_id=p.process_id where m.activity_type='start' and p.activity_type='end' group by m.machine_id;